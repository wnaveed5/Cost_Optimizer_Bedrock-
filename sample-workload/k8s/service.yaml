apiVersion: v1
kind: Service
metadata:
  name: ecommerce-app-service
  namespace: default
  labels:
    app: ecommerce-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: ecommerce-app 